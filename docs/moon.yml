$schema: "https://moonrepo.dev/schemas/project.json"
type: tool
language: shell

tasks:
  gen:
    command: bash
    args: ["../scripts/docs-gen.sh"]
    inputs:
      - "../rust-toolchain.toml"
    outputs:
      - "../README.md"
    options:
      cache: false
